namespace LeanHttp

/-- The LeanHttp package version, also used in the default user agent.
    `LeanHttp.version` reports the loaded libcurl version instead. -/
def packageVersion : String := "0.3.1"

end LeanHttp
